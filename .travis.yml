sudo: required
services:
  - docker

before_install:
  - docker build -t cbadez10/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true cbadez10/docker-react npm run test -- --coverage
